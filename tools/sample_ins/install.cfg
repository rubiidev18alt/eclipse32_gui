# Eclipse32 .INS — install manifest (parsed by the `install` shell command)
# DEST   — target directory on the FAT32 volume (default /apps)
# APP    — display name (optional)
# VERSION — free-form version string (optional)

DEST=/apps
APP=HELLO
VERSION=1.0
